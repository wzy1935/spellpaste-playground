{
  "spells": [
    {
      "trigger": "hello",
      "description": "A simple spell generate \"Hello world!\"",
      "tags": [],
      "entry": {
        "default": "echo 'Hello world!'"
      },
      "settings": {
        "outputMode": "preview"
      }
    },
    {
      "trigger": "upper case",
      "description": "Convert all letters to upper case",
      "entry": {
        "default": "python upper.py"
      }
    },
    {
      "trigger": "stream test",
      "description": "stream test",
      "entry": {
        "default": "python stream.py"
      },
      "settings": {
        "outputMode": "preview"
      }
    }
  ]
}